# SCV Source-Knowledge Synthetic Fixtures v1

## Canonical Surfaces

- `knowledge/scv/fixtures/v1/MANIFEST.md`
- `knowledge/scv/fixtures/v1/source-knowledge.json`

## Evidence Scope

The fixture is authored synthetic material, not a retained Google, Cloudflare or other vendor statement. Its unreachable example.invalid locators are provenance labels and were not fetched. Source planning, pure transition, capture, interpretation, graph, query and explanation objects were produced by the development SCV engine. The explicit query times demonstrate the same graph before and after selected freshness expiry. Generation executes no source text or network operation, commits no protected source/graph state and claims no deployed acceptance.

The keys `source_plan`, `source_transition`, `capture`, `knowledge`, `graph`, `query_result`, `expired_query_result` and `explain_result` correspond to the exact v1 schema definitions in `knowledge/scv/schemas/v1/`. `source_plan_input`, `capture_input` and `interpretation_input` are the reproducible owner-operation inputs. The enclosing fixture object is test/documentation organization, not a new runtime wire protocol. `knowledge/scv/SOURCE-KNOWLEDGE.md` owns its semantics.
