{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:symphony:ssiag:provider-binding-recovery-request:v1",
  "title": "SSIAG provider binding recovery request v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["expected_state_digest", "reason"],
  "properties": {
    "expected_state_digest": {"$ref": "#/$defs/digest_or_absent"},
    "reason": {"type": "string", "minLength": 1, "maxLength": 1024, "pattern": "^[^\\u0000\\r\\n]+$"}
  },
  "$defs": {
    "digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "digest_or_absent": {"oneOf": [{"const": "absent"}, {"$ref": "#/$defs/digest"}]}
  }
}
