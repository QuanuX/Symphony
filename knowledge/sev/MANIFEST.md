# Symphony Evolution Vector Manifest

## Canonical Target

`knowledge/sev/`

## Identity

SEV is the Symphony Evolution Vector. SCSEV is its qxctl command-surface evolution profile.

## Classification

- autonomous Symphony Knowledge Vector contract surface;
- canonical evolution-case, disposition, impact, verification, and novelty procedure;
- freezing-path planning and evidence capability;
- not an authority service, canonical mutator, package manager, scheduler, command registry, feature registry, audit ledger, or database.

## Declared Contract Truth Role

SEV owns:

- `sevcase:` and `sevdisp:` identities;
- append-forward evolution case semantics;
- impact assessment and dependency graph rules;
- deterministic dispositions and ready sets;
- reobservation, recalculation, verification, recovery, and closure rules;
- SCSEV command-surface consequence coverage;
- watch/session and novelty profiles; SAV separately owns Extension Capsule and Installation Blueprint composition truth.
- evolution-session bindings that attach exact case and CURRENT digests to the existing coordinator lifecycle stream without creating a parallel journal.

## Canonical Surface

```text
knowledge/sev/
  INTENT.md
  MANIFEST.md
  SKILL.md
  SPEC.md
  DISPOSITIONS.md
  NOVELTY.md
  WATCH.md
  profiles/
    qxctl-command-surface.md
  schemas/v1/
```

## Relationships

- SAV supplies exact CURRENT and accord evidence.
- SSFV supplies feature and administration truth.
- qxctl supplies expected and observed command truth and performs separately authorized external actions.
- Engine descriptors supply stable operation evidence.
- The knowledge coordinator owns durable noncanonical transition journals.
- SSIAG owns permission; STAV owns safe audit; Maestro owns receptor presence; STSC owns time.
- SKVI routes canonical surfaces; SCLV records merged changes; SODV governs publication.

## Installability

The C++26 implementation lives at `modules/sev-engine/`, installs as `sev-engine`, executes as `symphony-sev`, and begins `installed_undocked`. Exact versions may coexist without newest-version inference.

## Status

Architect-ratified contract and implementation program. Runtime behavior is claimed only after implemented proof.

## Non-Authorization Statement

This manifest authorizes no canonical apply, invented remediation identity, persistent canonical graph, arbitrary action reordering, native Windows engine, hot/warm dependency, or release claim.
