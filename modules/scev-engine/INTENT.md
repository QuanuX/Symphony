# Symphony Cloud Edge Vector Engine Intent

Provide independently installable C++26 source-knowledge computation for `scev` in the freezing path. Edge-provider family knowledge, preserving distinct product and service boundaries. The shared implementation preserves native source meaning, durable provenance supplied by its storage adapter and reproducible graph projections. It is useful without a parent engine, provider credentials, an AI service, a database server or a network listener.

The engine computes source transitions and knowledge results. Protected persistence adapters own their declared filesystem transactions; an engine transition alone is neither authorization nor a durable commit. qxctl presents those operations without replacing domain semantics.

For agent-operated profile preparation, exact schema discovery and retained evidence workflows, read `knowledge/scv/AGENT-WORKFLOWS.md`. Domain semantics stay in the C++ owner; qxctl supplies the operating interface. Templates require explicit caller input, and missing evidence remains unresolved.

Use the `0.5.0-dev` provider coverage interface in `knowledge/scv/COVERAGE.md` to inspect the declared inventory, selected acquisition evidence and supplied interpretation coverage independently. Preserve caller scope and unknowns.
